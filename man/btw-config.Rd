% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/btw-config.R
\name{btw-config}
\alias{btw-config}
\title{Where btw looks for configuration}
\description{
\pkg{btw} reads configuration -- context files (\code{btw.md}, \code{AGENTS.md},
\code{CLAUDE.md}), skills, and btw-style agent definitions -- from a combination
of project-level and user-level (global) locations. This page describes
where btw looks for each type of configuration and in what order of
priority.
}
\section{User (global) locations}{


btw's user-level home is the \verb{~/.btw/} directory. Skills and btw-style agents
live there (and in two additional directories), while the user-level \code{btw.md}
configuration file may also live directly in your home directory.

The user-level directories, in decreasing order of priority, are:
\enumerate{
\item \verb{~/.btw/} (recommended)
\item \verb{~/.config/btw/}
\item The platform-specific R user config directory,
\code{tools::R_user_dir("btw")}
}

Skills are discovered in the \verb{skills/} subdirectory of each (e.g.
\verb{~/.btw/skills/}), and btw-style agents are discovered both as
\verb{agent-*.md} files directly inside each (e.g. \verb{~/.btw/agent-my_agent.md})
and as \verb{*.md} files in an \verb{agents/} subdirectory of each (e.g.
\verb{~/.btw/agents/my_agent.md}).

The user-level \code{btw.md} \strong{configuration file} is searched for in a slightly
different order: a \code{btw.md} directly in your home directory (\verb{~/btw.md})
takes precedence, followed by \code{btw.md} in each of the directories above
(\verb{~/.btw/btw.md}, \verb{~/.config/btw/btw.md}, \code{tools::R_user_dir("btw")/btw.md}).
\verb{~/btw.md} remains a first-class location; \code{use_btw_md("user")} creates new
configuration in the recommended \verb{~/.btw/} directory. If more than one
user-level \code{btw.md} exists, the highest-priority one is used and btw warns
once per session.

On Windows, R's notion of your home directory (\code{fs::path_home_r()},
typically your \code{Documents} folder) can differ from your user profile
directory (\code{fs::path_home()}); btw searches both.
}

\section{Project locations}{


Project-level configuration is discovered by walking up from the current
working directory to the project root, where the project root is
identified by the presence of a \code{DESCRIPTION}, \code{.git}, \code{.vscode}, \code{.here},
or \verb{*.Rproj} file.
\itemize{
\item \strong{Config file}: the nearest \code{btw.md} file, or if none is found, the
nearest \code{AGENTS.md} file, or if neither is found, the nearest \code{CLAUDE.md}
file. (\code{btw.md} > \code{AGENTS.md} > \code{CLAUDE.md})
\item \strong{btw-style agents}: \verb{.btw/agents/*.md} or \verb{.btw/agent-*.md}
\item \strong{Skills}: \verb{.btw/skills/} or \verb{.agents/skills/}
}
}

\section{Claude Code agents}{


As an intentional special case -- distinct from, and not derived from, the
user (global) locations described above -- btw also discovers
\href{https://docs.claude.com/en/docs/claude-code/sub-agents}{Claude Code sub-agent}
definitions from:
\itemize{
\item \verb{.claude/agents/} in the project
\item \verb{~/.claude/agents/} for the user
}

This deliberately mirrors Claude Code's own convention for locating
sub-agents, rather than following btw's usual user-level directory
priority.
}

