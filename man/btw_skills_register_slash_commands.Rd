% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool-skills.R
\name{btw_skills_register_slash_commands}
\alias{btw_skills_register_slash_commands}
\title{Register skill slash commands in a chat}
\usage{
btw_skills_register_slash_commands(chat, reserved = character())
}
\arguments{
\item{chat}{The chat handle returned by \code{shinychat::chat_server()}.}

\item{reserved}{Skill names to skip, as a character vector. Pass the names
of slash commands you registered yourself so a skill can't take their
place. btw_app(), for example, passes its own \verb{/new} and \verb{/clear}
commands.}
}
\value{
\code{chat}, invisibly.
}
\description{
Registers a slash command for every skill discovered by the skill tool,
using the chat handle returned by \code{shinychat::chat_server()}. Requires
shinychat 0.4.0.9000 or later.

Each skill is registered under its own name, e.g. \verb{/skill-creator}. When the
user submits the command, btw sends the skill's full instructions to the
model, followed by the text the user typed after the command. The chat UI
shows \verb{/skill-creator <user text>}; the model receives the skill's
instructions and the user text separated by a blank line.

If the command fails, btw restores the original slash text to the chat
input and shows a toast with the error.

Skill commands can't run while a response is streaming; in that case the
command fails the same way, restoring the input and showing an error toast.

Slash command names may only contain letters, numbers, underscores, and
hyphens. Skills with other names are skipped with a warning, as are skills
whose names match btw's own \verb{/btw-*} slash commands and any names passed to
\code{reserved}.
}
\examples{
\dontshow{if (rlang::is_installed("shinychat", version = "0.5.0")) withAutoprint(\{ # examplesIf}
server <- shinychat::chat_server("chat", client = ellmer::chat_openai())
btw_skills_register_slash_commands(server)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=btw_tool_skill]{btw_tool_skill()}} for a skill's full instructions and
\link{btw-config} for the skill discovery locations.

Other skills:
\code{\link[=btw_skill_install_github]{btw_skill_install_github()}},
\code{\link[=btw_skill_install_package]{btw_skill_install_package()}},
\code{\link[=btw_skill_install_project]{btw_skill_install_project()}},
\code{\link[=btw_skill_prompt]{btw_skill_prompt()}},
\code{\link[=btw_tool_skill]{btw_tool_skill()}}
}
\concept{skills}
