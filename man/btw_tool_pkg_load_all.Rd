% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool-pkg-devtools.R
\name{btw_tool_pkg_load_all}
\alias{btw_tool_pkg_load_all}
\title{Tool: Load package code}
\usage{
btw_tool_pkg_load_all(pkg = ".", `_intent` = "")
}
\arguments{
\item{pkg}{Path to package directory. Defaults to '.'. Must be within
current working directory.}

\item{_intent}{An optional string describing the intent of the tool use.
When the tool is used by an LLM, the model will use this argument to
explain why it called the tool.}
}
\value{
The output from \code{\link[pkgload:load_all]{pkgload::load_all()}}.
}
\description{
Load package code using \code{\link[pkgload:load_all]{pkgload::load_all()}} in a separate R process via
\code{\link[callr:r]{callr::r()}}. This verifies that the package code loads without syntax errors
and triggers recompilation of any compiled code (C, C++, etc.).
}
\details{
\strong{Important:} This tool runs \code{load_all()} in an isolated R process and does
NOT load the package code into your current R session. If you need to load
the package code in your current session for interactive use, use the run R
code tool to call \code{pkgload::load_all()} directly.
}
\seealso{
\code{\link[=btw_tools]{btw_tools()}}

Other pkg tools: 
\code{\link{btw_tool_pkg_check}()},
\code{\link{btw_tool_pkg_coverage}()},
\code{\link{btw_tool_pkg_document}()},
\code{\link{btw_tool_pkg_test}()}
}
\concept{pkg tools}
