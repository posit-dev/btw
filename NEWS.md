# btw (development version)

* `btw_app()` now works correctly with `{ellmer}` v0.4.0 (#121).

* `btw_app()` now shows 4 decimal places in the cost status when less than $0.01 (#121).

# btw 1.0.0

* Initial CRAN submission.
